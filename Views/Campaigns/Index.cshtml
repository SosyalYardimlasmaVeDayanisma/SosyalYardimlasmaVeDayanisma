@model List<Campaign>
@{
    ViewData["Title"] = "Dayanışma Alanları";
    var search = ViewBag.Search as string;
    var city = ViewBag.City as string;
    var disasterType = ViewBag.DisasterType as DisasterType?;
    var status = ViewBag.Status as CampaignStatus?;
    var category = ViewBag.Category as CampaignCategory?;
    var cities = ViewBag.Cities as string[];
    var disasterTypes = ViewBag.DisasterTypes as DisasterType[];
    var statuses = ViewBag.Statuses as CampaignStatus[];
    var categories = ViewBag.Categories as CampaignCategory[];
}

<div class="container page-container">
    <!-- Başlık ve Açıklama -->
    <div class="mb-8" style="text-align: center; max-width: 800px; margin: 0 auto 48px;">
        <h1 class="page-title">Dayanışma Alanları</h1>
        <p class="page-description" style="font-size: 16px; line-height: 1.6;">
            Destek vermek istediğin alanı seç. Her katkı bir fark yaratır, hiçbir bağış küçük değildir.
        </p>
    </div>

    <!-- Alan Filtreleri -->
    <div class="goodness-areas-filter">
        <button class="area-filter-btn @(category == null ? "active" : "")" data-category="">
            <span class="area-icon">🌟</span>
            <span class="area-name">Tümü</span>
        </button>
        @foreach (CampaignCategory cat in categories ?? Array.Empty<CampaignCategory>())
        {
            var (displayName, icon) = cat switch
            {
                CampaignCategory.AfetAcilDurum => ("Afet & Acil", "🚨"),
                CampaignCategory.EgitimCocuk => ("Eğitim & Çocuk", "📚"),
                CampaignCategory.Saglik => ("Sağlık", "🏥"),
                CampaignCategory.Hayvanlar => ("Hayvanlar", "🐾"),
                CampaignCategory.GidaTemelIhtiyac => ("Gıda & İhtiyaç", "🍲"),
                CampaignCategory.CevreVeDoga => ("Çevre & Doğa", "🌱"),
                _ => (cat.ToString(), "📋")
            };
            <button class="area-filter-btn @(category == cat ? "active" : "")" data-category="@cat">
                <span class="area-icon">@icon</span>
                <span class="area-name">@displayName</span>
            </button>
        }
    </div>

    <!-- Gelişmiş Filtreler (Daraltılabilir) -->
    <div class="advanced-filters-toggle" onclick="toggleAdvancedFilters()">
        <span>Gelişmiş Filtreler</span>
        <svg class="toggle-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
        </svg>
    </div>

    <div class="advanced-filters-container" id="advancedFilters" style="display: none;">
        <form method="get" action="/Campaigns" id="filterForm">
            <input type="hidden" name="category" id="categoryInput" value="@category">
            
            <div class="search-input-wrapper">
                <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                </svg>
                <input type="text" name="search" value="@search" placeholder="Kampanya ara..." class="search-input form-input">
            </div>
            
            <div class="filters-grid">
                <select name="city" class="form-select">
                    <option value="">Tüm Şehirler</option>
                    @foreach (var c in cities ?? Array.Empty<string>())
                    {
                        <option value="@c" selected="@(city == c)">@c</option>
                    }
                </select>

                <select name="disasterType" class="form-select">
                    <option value="">Tüm Afet Türleri</option>
                    @foreach (var dt in disasterTypes ?? Array.Empty<DisasterType>())
                    {
                        <option value="@dt" selected="@(disasterType == dt)">@dt</option>
                    }
                </select>

                <select name="status" class="form-select">
                    <option value="">Tüm Durumlar</option>
                    @foreach (var s in statuses ?? Array.Empty<CampaignStatus>())
                    {
                        <option value="@s" selected="@(status == s)">@s</option>
                    }
                </select>
            </div>

            <div class="filter-actions">
                <button type="submit" class="btn btn-primary">Filtrele</button>
                <a href="/Campaigns" class="btn btn-outline">
                    <svg style="width: 16px; height: 16px; margin-right: 8px;" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                    </svg>
                    Temizle
                </a>
            </div>
        </form>
    </div>

    <div class="campaigns-section">
        @if (Model != null && Model.Any())
        {
            <div class="campaigns-list" id="campaignsList">
                @foreach (var campaign in Model)
                {
                    @await Html.PartialAsync("_DonationCard", campaign)
                }
            </div>
        }
        else
        {
            <div class="card empty-state">
                <p>
                    @(Model == null ? "Kampanyalar yükleniyor..." : "Seçili kategoride kampanya bulunamadı.")
                </p>
            </div>
        }
    </div>
</div>

<script>
    // Alan filtreleme - Anında çalışır
    document.querySelectorAll('.area-filter-btn').forEach(btn => {
        btn.addEventListener('click', function() {
            const selectedCategory = this.dataset.category;
            document.getElementById('categoryInput').value = selectedCategory;
            
            // Aktif durumu güncelle
            document.querySelectorAll('.area-filter-btn').forEach(b => b.classList.remove('active'));
            this.classList.add('active');
            
            // Formu gönder
            document.getElementById('filterForm').submit();
        });
    });

    // Gelişmiş filtreleri aç/kapa
    function toggleAdvancedFilters() {
        const filters = document.getElementById('advancedFilters');
        const icon = document.querySelector('.toggle-icon');
        
        if (filters.style.display === 'none') {
            filters.style.display = 'block';
            icon.style.transform = 'rotate(180deg)';
        } else {
            filters.style.display = 'none';
            icon.style.transform = 'rotate(0deg)';
        }
    }
</script>
